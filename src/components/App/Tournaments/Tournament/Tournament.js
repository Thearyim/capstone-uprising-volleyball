import React from 'react';

const Tournament = ({ info, onSubmit }) => {
    return(
      <div>
        <h5>info.name</h5>
        <div>info.description</div>
      </div>
    );
}

export default Tournament;
